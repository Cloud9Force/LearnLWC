import { LightningElement } from 'lwc';

export default class PrimitiveParent extends LightningElement {
    // Primitive value owned by the parent
    parentMessage = 'Hello from Primitive Parent';
}